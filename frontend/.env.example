# Vite API Base URL for backend communication
VITE_API_URL=http://localhost:8000

# Optional Ollama connection URL if direct client-side integration is needed (typically goes through backend instead)
VITE_OLLAMA_URL=http://localhost:11434
